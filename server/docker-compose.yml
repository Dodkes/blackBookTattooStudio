services:
  app:
    build: .
    container_name: black-app
    ports:
      - "3000:3000"
    restart: unless-stopped

    # unless-stopped - kontajner sa samospusti po restarte docker demona alebo systemu
